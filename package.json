{
  "name": "skedoodle",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"pnpm --filter skedoodle-client dev\" \"pnpm --filter skedoodle-server dev\"",
    "build": "pnpm --filter skedoodle-client build && pnpm --filter skedoodle-server build",
    "lint": "pnpm --filter skedoodle-client lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sqlite3"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
